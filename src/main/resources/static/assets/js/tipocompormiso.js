let response = { "status":"error", "status_code":409, "message":"Validations failed, see validations for more details", "validations":{ "nombre":"The name field is required.", "descripcion":"The field email user must be between 6 and 120 characters in length", "estado":"The password user field is required."}, "data":null }; // Solo si hay error if(response.status == 'error') { // Colocar mensaje en formulario document.querySelector('#message-error').innerText = response.message; // Recorrer validaciones por clave for(let key in response.validations) { // Asignar mensaje al span correspondiente document.querySelector(#${key}-error).innerText = response.validations[key]; } }